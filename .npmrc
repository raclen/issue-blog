# Cloudflare Workers Builds / Pages may run with NODE_ENV=production,
# which makes `npm ci` skip devDependencies. This project needs
# devDependencies at build/deploy time (wrangler, pagefind, astro-pagefind,
# vite, @tailwindcss/typography), so force them to be installed.
include=dev
